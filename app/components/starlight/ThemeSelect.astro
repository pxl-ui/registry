---

import { Button } from "@/components/ui/pxl/button";

---

<starlight-theme-select>
  <Button variant="ghost" size="icon">
    <svg data-theme-selector="light" class="flex" xmlns="http://www.w3.org/2000/svg" fill="currentColor" viewBox="0 0 24 24"><path d="M13 22h-2v-3h2v3Zm-6-3H5v-2h2v2Zm12 0h-2v-2h2v2Zm-4-2H9v-2h6v2Zm-6-2H7V9h2v6Zm8 0h-2V9h2v6ZM5 13H2v-2h3v2Zm17 0h-3v-2h3v2Zm-7-4H9V7h6v2ZM7 7H5V5h2v2Zm12 0h-2V5h2v2Zm-6-2h-2V2h2v3Z"/></svg>
    <svg data-theme-selector="dark" class="hidden" xmlns="http://www.w3.org/2000/svg" fill="currentColor" viewBox="0 0 24 24"><path d="M18 22H8v-2h10v2ZM8 20H6v-2h2v2Zm12 0h-2v-2h2v2ZM6 18H4v-2h2v2Zm16 0h-2v-4h-2v-2h2v-2h2v8ZM4 16H2V6h2v10Zm14 0h-6v-2h6v2Zm-6-2h-2v-2h2v2Zm-2-2H8V6h2v6ZM6 6H4V4h2v2Zm8-2h-2v2h-2V4H6V2h8v2Z"/></svg>
  </Button> 
</starlight-theme-select>

<script is:inline>
	StarlightThemeProvider.updatePickers();
</script>

<script>
	type Theme = 'dark' | 'light';

	const storageKey = 'starlight-theme';

	const parseTheme = (theme: unknown): Theme =>
		theme === 'dark' || theme === 'light' ? theme : 'light';

	const loadTheme = (): Theme =>
		parseTheme(typeof localStorage !== 'undefined' && localStorage.getItem(storageKey));

	function storeTheme(theme: Theme): void {
		if (typeof localStorage !== 'undefined') {
			localStorage.setItem(storageKey, theme === 'light' || theme === 'dark' ? theme : '');
		}
	}

	function onThemeChange(theme: Theme): void {
		StarlightThemeProvider.updatePickers(theme);
		document.documentElement.dataset.theme = theme;

    if (theme === "light") {
      (document.querySelector("[data-theme-selector=light]") as SVGElement)?.classList.add("flex");
      (document.querySelector("[data-theme-selector=light]") as SVGElement)?.classList.remove("hidden");
      (document.querySelector("[data-theme-selector=dark]") as SVGElement)?.classList.remove("flex");
      (document.querySelector("[data-theme-selector=dark]") as SVGElement)?.classList.add("hidden");
    } else {
      (document.querySelector("[data-theme-selector=dark]") as SVGElement)?.classList.add("flex");
      (document.querySelector("[data-theme-selector=dark]") as SVGElement)?.classList.remove("hidden");
      (document.querySelector("[data-theme-selector=light]") as SVGElement)?.classList.remove("flex");
      (document.querySelector("[data-theme-selector=light]") as SVGElement)?.classList.add("hidden");
    }

		storeTheme(theme);
	}

	class StarlightThemeSelect extends HTMLElement {
		constructor() {
			super();
			onThemeChange(loadTheme());
			this.querySelector('button')?.addEventListener('click', (e) => {
        onThemeChange(
          document.documentElement.dataset.theme === "light" ?
            "dark" : "light"
        );
			});
		}
	}
	customElements.define('starlight-theme-select', StarlightThemeSelect);
</script>