---
title: Widgets
sidebar:
  order: 7
description: Here you can find all the widgets available in the library. We are working on adding more widgets.
---
import { LinkCard } from "~/components";
import { lists } from "~/lib/widgets";
import { toRouteId } from "~/lib/registry";

<div class="not-content mt-8 grid grid-cols-2 gap-4 md:grid-cols-3">

  {lists.medium.map(item => (
    <LinkCard 
      href={toRouteId(item.name)} 
      title={item.title ?? item.name}
      description={item.description}
    >
    </LinkCard>
  ))}

</div>