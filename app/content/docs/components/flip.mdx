---
title: Flip
description: A flip component that supports automatic, manual (gesture-based), and programmatic flipping.
---
import { Example, InstallCommand } from '~/components';

import Preview from '~/examples/flip/preview.tsx';
import PreviewRaw from '~/examples/flip/preview.tsx?raw';

import ManualTrigger from '~/examples/flip/manual-trigger.tsx';
import ManualTriggerRaw from '~/examples/flip/manual-trigger.tsx?raw';

A flip component that supports automatic, manual (gesture-based), and programmatic flipping.

<Example code={PreviewRaw}>
  <Preview client:load />
</Example>

<InstallCommand item="flip" />

## Manual Trigger

<Example code={ManualTriggerRaw}>
  <ManualTrigger client:load />
</Example>