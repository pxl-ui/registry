---
title: Radio Group
description: A set of checkable buttons where no more than one can be checked at a time.
---
import { Example, InstallCommand } from '~/components';

import Preview from '~/examples/radio-group/preview.tsx';
import PreviewRaw from '~/examples/radio-group/preview.tsx?raw';

import Variants from '~/examples/radio-group/variants.tsx';
import VariantsRaw from '~/examples/radio-group/variants.tsx?raw';

A set of checkable buttons where no more than one can be checked at a time.

<Example code={PreviewRaw}>
  <Preview client:load />
</Example>

<InstallCommand item="radio-group" />

## Variants

<Example code={VariantsRaw}>
  <Variants client:load />
</Example>