---
title: Components
description: Here you can find all the components available in the library. We are working on adding more components.
sidebar:
  order: 6
---
import { LinkCard } from "~/components";
import { lists } from "~/lib/components";
import { toRouteId } from "~/lib/registry";

## Layout
> <p className="text-muted-foreground">How do I organize the interface?</p>


<div class="not-content mt-8 grid grid-cols-2 gap-4 md:grid-cols-3">

  {lists.layout.map(item => (
    <LinkCard 
      href={toRouteId(item.name, "layout")} 
      title={item.title ?? item.name}
      description={item.description}
    >
    </LinkCard>
  ))}

</div>

## Interaction
> <p className="text-muted-foreground">How does the user interact with it?</p>

<div class="not-content mt-8 grid grid-cols-2 gap-4 md:grid-cols-3">

  {lists.interaction.map(item => (
    <LinkCard 
      href={toRouteId(item.name, "interaction")} 
      title={item.title ?? item.name}
      description={item.description}
    >
    </LinkCard>
  ))}

</div>

## Navigation
> <p className="text-muted-foreground">How does the user move through it?</p>

<div class="not-content mt-8 grid grid-cols-2 gap-4 md:grid-cols-3">

  {lists.navigation.map(item => (
    <LinkCard 
      href={toRouteId(item.name, "navigation")} 
      title={item.title ?? item.name}
      description={item.description}
    >
    </LinkCard>
  ))}

</div>

## Content
> <p className="text-muted-foreground">What does the interface show?</p>

<div class="not-content mt-8 grid grid-cols-2 gap-4 md:grid-cols-3">

  {lists.content.map(item => (
    <LinkCard 
      href={toRouteId(item.name, "content")} 
      title={item.title ?? item.name}
      description={item.description}
    >
    </LinkCard>
  ))}

</div>