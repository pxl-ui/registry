---
title: Features
description: Here you can find all the feature blocks available in the library. We are working on adding more features.
---
import { LinkCard } from "~/components";
import { lists } from "~/lib/features";
import { toRouteId } from "~/lib/registry";

## Magazine
> <p className="text-muted-foreground">Components to display blog posts or articles contents</p>


<div class="not-content mt-8 grid grid-cols-2 gap-4 md:grid-cols-3">

  {lists.articles.map(item => (
    <LinkCard 
      href={toRouteId(item.name)} 
      title={item.title ?? item.name}
      description={item.description}
    >
    </LinkCard>
  ))}

</div>

## Newspaper
> <p className="text-muted-foreground">Components to display distraction-free RSS feeds</p>


<div class="not-content mt-8 grid grid-cols-2 gap-4 md:grid-cols-3">

  {lists.feeds.map(item => (
    <LinkCard 
      href={toRouteId(item.name)} 
      title={item.title ?? item.name}
      description={item.description}
    >
    </LinkCard>
  ))}

</div>

## Conversation
> <p className="text-muted-foreground">Components to display chat conversations and messages</p>


<div class="not-content mt-8 grid grid-cols-2 gap-4 md:grid-cols-3">

  {lists.chats.map(item => (
    <LinkCard 
      href={toRouteId(item.name)} 
      title={item.title ?? item.name}
      description={item.description}
    >
    </LinkCard>
  ))}

</div>

## Marketing
> <p className="text-muted-foreground">Components to display landing page and marketing contents</p>


<div class="not-content mt-8 grid grid-cols-2 gap-4 md:grid-cols-3">

  {lists.marketing.map(item => (
    <LinkCard 
      href={toRouteId(item.name)} 
      title={item.title ?? item.name}
      description={item.description}
    >
    </LinkCard>
  ))}

</div>