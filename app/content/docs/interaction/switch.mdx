---
title: Switch
---
import { ComponentPreview, Install } from '~/components';

<ComponentPreview item="switch" example="demo" />

## Installation

<Install item="switch" />

## Usage

```tsx
import { Switch } from "@/components/ui/pxl/switch"
```

```tsx
<Switch />
```

## Size

Use the `size` prop to change the size of the switch.

<ComponentPreview item="switch" example="sizes" />

## Variants

Use the `variant` prop to change the colors of the switch.

<ComponentPreview item="switch" example="variants" />

## Disabled

Add the `disabled` prop to the `Switch` component to disable the switch. Add the `data-disabled` prop to the `Field` component for styling.

## Invalid

Add the `aria-invalid` prop to the `Switch` component to indicate an invalid state. Add the `data-invalid` prop to the `Field` component for styling.

## API Reference

See the [Base UI Switch](https://base-ui.com/react/components/switch#api-reference) documentation.
